# Public-safe example.
# Review and update trusted proxy ranges in your private repository.

# For Cloudflare-like reverse proxy:
# real_ip_header CF-Connecting-IP;

# For generic reverse proxy or load balancer:
# real_ip_header X-Forwarded-For;

# Example placeholder only:
# set_real_ip_from 203.0.113.0/24;
